name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  hassfest:
    name: Hassfest validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: home-assistant/actions/hassfest@master

  tests:
    name: Tests (pytest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install homeassistant==2025.11.1
          pip install pytest-homeassistant-custom-component==0.13.295
          pip install pytest pytest-asyncio pytest-cov
          pip install pycares==4.5.0 aiodns==3.1.1 --force-reinstall
          
      - name: Run tests with coverage
        continue-on-error: true
        run: |
          pytest -v --cov=custom_components/cronostar --cov-report=term-missing | tee pytest-coverage.txt
          
      - name: Extract Coverage
        id: extract_coverage
        run: |
          # Use -a to avoid issues with non-text characters and tail -n 1 for the last match
          COVERAGE=$(grep -a "TOTAL" pytest-coverage.txt | tail -n 1 | awk '{print $NF}' | tr -d '%' || echo "0")
          if [ -z "$COVERAGE" ]; then
            echo "Warning: Could not extract coverage, setting to 0"
            COVERAGE=0
          fi
          echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT
          
      - name: Create Coverage Badge
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_SECRET }}
          gistID: ${{ secrets.GIST_ID }}
          filename: cronostar_coverage.json
          label: Coverage
          message: ${{ steps.extract_coverage.outputs.coverage }}%
          valColorRange: ${{ steps.extract_coverage.outputs.coverage }}
          maxColorRange: 100
          minColorRange: 0
          forceUpdate: true
